"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.convertArrayOfArraysToCSV = void 0;

var _checkSpecialCharsAndEmpty = require("../helpers/check-special-chars-and-empty");

function _toConsumableArray(arr) { return _arrayWithoutHoles(arr) || _iterableToArray(arr) || _nonIterableSpread(); }

function _nonIterableSpread() { throw new TypeError("Invalid attempt to spread non-iterable instance"); }

function _iterableToArray(iter) { if (Symbol.iterator in Object(iter) || Object.prototype.toString.call(iter) === "[object Arguments]") return Array.from(iter); }

function _arrayWithoutHoles(arr) { if (Array.isArray(arr)) { for (var i = 0, arr2 = new Array(arr.length); i < arr.length; i++) { arr2[i] = arr[i]; } return arr2; } }

var convertArrayOfArraysToCSV = function convertArrayOfArraysToCSV(data, _ref) {
  var header = _ref.header,
      separator = _ref.separator;

  var array = _toConsumableArray(data);

  var csv = '';

  if (header) {
    header.forEach(function (headerEl, i) {
      var thisHeaderEl = headerEl || '';
      var includesSpecials = (0, _checkSpecialCharsAndEmpty.checkSpecialCharsAndEmpty)(thisHeaderEl);
      csv += (includesSpecials ? "\"".concat(thisHeaderEl, "\"") : thisHeaderEl) + (Object.keys(header).length - 1 === i ? '' : separator) + (Object.keys(header).length - 1 === i ? '\n' : '');
    });
  }

  array.forEach(function (row) {
    row.forEach(function (value, i) {
      var thisValue = value || (value === 0 ? 0 : '');
      var includesSpecials = (0, _checkSpecialCharsAndEmpty.checkSpecialCharsAndEmpty)(thisValue);
      csv += (includesSpecials ? "\"".concat(thisValue, "\"") : thisValue) + (row.length - 1 === i ? '' : separator) + (row.length - 1 === i ? '\n' : '');
    });
  });
  return csv;
};

exports.convertArrayOfArraysToCSV = convertArrayOfArraysToCSV;